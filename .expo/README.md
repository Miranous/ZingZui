# .expo Directory

This directory contains Expo-specific files and caches.

## Files

- `devices.json` - List of devices that have connected to this project
- `types/` - Auto-generated TypeScript type definitions
- `web/` - Web-specific cache files

## Cache

The `web/cache/` directory contains cached assets for web builds to improve build performance.
